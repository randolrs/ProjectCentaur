// Drizzle schema for the Supabase Postgres database.
//
// Intentionally empty for M0 (skeleton). Table definitions begin in M1
// (auth + deterministic onboarding) — see SPEC.md "Key Data Structures".
// Keeping this file present lets drizzle.config.ts resolve and lets M1
// drop tables in without touching tooling.

export {};
